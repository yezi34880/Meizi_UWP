<Page
    x:Class="Meizi.CollectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Meizi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:DBHelper.Model"
    mc:Ignorable="d" >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <GridView  x:Name="mainContent"  SelectionChanged="mainContent_SelectionChanged" ItemsSource="{x:Bind listUrls}" >
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="model:Collection">
                    <Image Width="236" Height="354" Tag="{x:Bind LinkUrl}" Source="{x:Bind ImageUrl}"></Image>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <ProgressRing Name="Loading"
                      Width="100"
                      Height="100"
                      IsActive="True">
        </ProgressRing>
    </Grid>
</Page>
